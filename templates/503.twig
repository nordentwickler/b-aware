{# ## Extends #}
{% extends '_layouts/main.twig' %}

{# ## Permissions #}

{# ## Macros #}

{# ## Data logic #}

{# ## Blocks #}
{% block content %}

  <div class="container">
    <div class="text-center  my-24">
      <h1 class="!mb-0 mb-0">504 - Seite nicht erreichbar.</h1>
    </div>
  </div>

{% endblock %}
